# Mobile implementation evidence

Tracks [spec #459](https://github.com/Zacplischka/dinner_app/issues/459). **Implementation is partial.** Public store publishing is excluded. Native readiness, physical-device verification and release approval are not implied by project generation or simulator checks.

Work runs in the isolated `codex/459-mobile-app` lane. Checkpoint `2d3c79a` preserves the tested prototype and is backed up on GitHub. Integration with main preserves the YupCrew branding, public privacy page and compatible domain cutover. The shared main checkout is untouched. Older evidence below refers to the Heykeen Test artifacts actually exercised at those checkpoints.

## Foundation milestone boundary — 8 September 2026

The user authorized checkpointing and merging a reviewed foundation while leaving the rest of spec #459 open. This milestone includes bundled native projects, configuration/build checks, shared browser-compatible recovery fixes, permissions/sharing adapters, secure native persistence, logging corrections and the recorded prototype evidence. Existing YupCrew browser behavior, privacy access and legacy-domain continuity must remain intact after integration. A merge requires independent standards/spec review and passing relevant CI, browser and native compile checks.

This is partial delivery, not a passed physical-device prototype gate or a store-ready app. Apple sign-in, verified linking, deletion, new safety controls, verified OS links, complete accessibility/device matrices and signed release operations remain outstanding. Existing Google/browser identity must continue to work. The prototype-first gate remains in force for the remaining expansion. Historical test binaries and their hashes are preserved; new source or branding is not retroactively covered by their device evidence.

## Implementation status

| Scope / stories | Current evidence and remaining work |
| --- | --- |
| Bundled platforms, configuration, branding (1–3, 58, 60) | Capacitor 8.5.1 iOS/Android projects bundle the existing interface. Release environment validation, native CSP, exact native server origins, secure-storage plugin and SwiftPM pins added. Debug/release IDs are separate. iOS simulator and Android Debug app/instrumentation APKs compile locally. A signed Personal Team test artifact is installed on the physical iPhone and launches after developer trust. Native icons/splash, final identities, signed distribution and deliberate device-family choices remain pending. |
| Guest and mixed-client Branch journeys (4–14, 35–39, 65) | Existing domain behavior retained. Browser Watch/collaborative journeys pass. A physical iPhone 12 Pro Max, Android API 36 emulator and browser complete the same 15-card Watch round and agree on Dunkirk as the Match. The iPhone and browser also complete live-provider Eat Out and Takeaway Sessions, agreeing on 11 Inch Pizza. Cook is covered below. Like/Undo, Ready, Restart and Leave are exercised. Four-Participant coverage and the required physical Android prototype remain unverified; the user has no Android phone available. |
| Sharing, links, second Session (15–22) | Native Share/Clipboard and canonical public URLs implemented. Owned-route validation, cold/warm delivery and existing second-Session confirmation reused. Association generation and honest missing-file 404s tested locally. Physical iPhone sharing displays the public domain and cancellation retains participation. Android emulator sharing displays the canonical result URL. The simulator also displays the public domain after the URL correction. Published associations and signed cold/warm links are unverified. |
| Recovery and connectivity (23–30, 66) | Durable minimal native state, OS-backed rejoin capability, hydration before routing, authoritative reconciliation, missed-round reset, explicit failed-resume admission, offline/retry messages and no blind mutation replay implemented. Server rejects revoked/unmatched capabilities before any membership mutation. Physical iPhone background return and process termination retain its Like and next card; Android emulator termination restores the completed Match. Same-socket name-claim loss is fixed. Both recover automatically from a 70-second local backend interruption with the same two Participants, original Host and Android Ready state. Android also retains Ready after 27 seconds in the background. Full network/expiry and physical Android matrices remain pending. |
| Permissions and handoffs (31–34) | Foreground/coarse location and manual fallback, external HTTPS system-browser handoff, native permissions and Android Debug-only loopback HTTP support implemented. The physical iPhone supplies location with Allow Once and Precise Off; the Android emulator supplies approximate location with coarse permission granted and fine denied. First denial retains the error/manual form on both platforms after platform-specific lifecycle fixes. A live lookup exposed bare-postcode ambiguity; structured AU postal-code requests now resolve 3000 and 0800, with native 3000 entry updating the browser's shared area. Live restaurant Decks load. The iPhone opens Uber Eats/DoorDash in the system browser and returns; Uber's verification challenge prevents claiming destination checkout access. Physical Android and the remaining permission matrix remain pending. |
| Shopping List and Cook View (35–39) | Physical iPhone/browser Cook Match, shared Claims/releases, independent Shopper access after Session expiry and private cooking progress through background/termination pass. Expiry testing exposed and fixed stale list controls; the updated iPhone and browser remove an already-open list/method when it expires. Native wake-lock calls pass unit checks; independent observation of OS idle behavior remains pending. |
| Comparison and Group Order | A live Comparison streams partial and completed menus to iPhone and browser, with 40 matched items, exactly one Apify run per platform and one Snapshot in an isolated local Supabase database. Both contribute basket items; Buyer lock, a synthetic fee split, native copy confirmation, system-browser handoff and saved Buyer/fee recovery pass. The recovered fee field initially appeared empty; the updated iPhone shows the saved amount. Physical Android and the full concurrency/recovery matrix remain pending. |
| Google/Apple and Profile continuity (40–46) | Google system-browser PKCE callback, secure auth storage and bounded guest-first initialization implemented; handshake reads current auth. Apple sign-in, verified linking, discovery identity and actual refresh/revocation validation remain pending. |
| Deletion and real auth/database evidence (47–49) | Pending implementation and isolated real-auth/database evidence. |
| Reporting, blocks, Participant and Shopper safety (50–52, 67–71) | Pending the specified ADR amendment, implementation and concurrency/expiry evidence. Existing absent-Participant removal is not the new strict Host-only moderation control. |
| Privacy, support and accessibility (53–57) | Native Back handling, safe-area corrections, offline text, bridge log suppression and Preferences required-reason privacy manifest added. [Current data/permission inventory](mobile-data-inventory.md) records source and simulator-manifest evidence, storage behavior and open processor/retention questions. HTTP/Caddy logs now exclude automatic URLs/headers and private list/claim fields. Main supplies the public privacy page and support contact. Final signed-binary inventory, native disclosure reconciliation, the public deletion route, VoiceOver/TalkBack and the full device audit remain pending. |
| Reviewer access (59, 72) | Pending ordinary synthetic fixtures, separate deletion accounts and two clean review runs. |
| CI, compatibility, operations (61–64) | Unsigned Android/iOS compile workflow added and checked locally; hosted execution remains unproved. ADR 0007 now defines compatibility for supported installed clients and requires a baseline at the first release. Signed workflows, release artifact/symbol provenance, an actual released-client baseline and operator runbooks remain pending. |

## Latest checkpoint — Android recovery and native builds, 8 September 2026

The Android API 36 emulator and browser completed a Takeaway early-finish journey to 11 Inch Pizza and a shared Group Order. The emulator retained Buyer status and the synthetic **4.00** fee after termination; the browser retained its **27.84** Share. This reused the earlier live-provider Snapshot and started no additional Apify runs.

Native automation exposed two further defects. Android Back on Home returned to a previous Lobby instead of minimizing; the shared native listener now handles Home explicitly, while leaving a Session screen for Home preserves participation. Cold native recovery after a missed Restart could retain selections, cursor and a basket reference because Restart returns to waiting before incrementing the round. An authoritative waiting Lobby now clears that state. The final APK cold-launched directly into the correct Lobby with the original Host and two Participants, empty selections, cursor zero and no old basket reference, without manual Retry. Android Ready also survived Home navigation and minimization.

Mutation recovery now remains gated until relevant authoritative basket reads finish. Generation checks reject stale asynchronous results after disconnect, Leave or a newer admission. Independent review identified races that were corrected before the source freeze. Five real HTTP/Socket.IO/Redis Lobby contracts pass, including missed Restart and stale-round rejection. Full frontend verification passes **686 tests across 57 files**, with typechecks and focused checks passing. An initial full run had a collection error; its isolated test and an unchanged complete rerun passed, and both logs are retained. Real packet-loss behavior is not established by these checks.

Both final native artifacts contain the reviewed source. The final Android APK was exercised as above. The signed Personal Team iPhone artifact was installed, normally launched and captured on Home; its app, WDA and Appium processes were stopped afterward, leaving the app installed. Evidence and hashes are in ignored `output/mobile/android-recovery/`, with the iPhone artifact at `iphone/App.app`. An additional all-Pass/Results-Back scenario was not executed because the browser Host tab became unavailable; it is not a pass. Physical Android, real native authentication and the broader matrices remain outstanding.

This checkpoint adds recovery, navigation, native build and compatibility evidence. It does **not** complete Apple sign-in, verified account linking, account deletion or the specified safety controls. Earlier sections below describe historical artifacts and checkpoints.

## Local verification — 8 September 2026

Evidence files are under ignored `output/mobile/` in this lane. They are local development evidence, not a public release record. Earlier checkpoints are preserved below; the resumed mixed-client verification section records the latest physical-device results.

- Workspace typecheck passes using the explicitly pinned root TypeScript 5.9.3 compiler. Full suite results: 664 frontend tests and 1,034 backend tests across unit, real Redis integration and HTTP/Socket.IO contracts. The contract suite includes exact native-origin acceptance and rejected resume with reused code, unchanged rosters and deliberate fresh admission.
- After final corrections, 14 focused auth/lifecycle/Session-guard tests pass. They cover duplicate completed auth callbacks, offline/online, owned invite delivery, listener cleanup and avoiding cold-launch replay after ordinary navigation. Final `mobile:sync` and ad-hoc simulator compilation pass; the Mac lock prevented another UI run of the last artifact.
- Mobile build configuration and association generation checks pass. Caddy 2.11.4 serving checks pass, including missing association 404 and JSON content.
- Playwright mobile-Chrome checks: Watch and collaborative-Session journeys pass, as do the Join and Create header scenarios. The full Restaurant header journey reaches the provider lookup but cannot finish with the deliberately dummy Places key in this local backend; this is an incomplete scenario, not a pass. The run exposed a blank location label (fixed with coordinate fallback) and a Ready helper race (fixed by waiting for the lazy page).
- Xcode 26.3 / iOS 26.2 SDK compiles the simulator app. Native checks use iPhone 17 Pro simulator, iOS 26.3.1, local backend/Redis, and deliberately blank Supabase settings. No physical-device pass is claimed.
- Simulator observations: guest Watch creation; simultaneous browser roster entry; recovery after process termination without a duplicate Participant; existing absent-Participant removal confirmation; share-sheet cancellation; public-domain invite preview; Home/Session safe areas. Full round completion, real location, Google/Apple callbacks and verified association delivery remain unproved.
- The current data/permission inventory includes actual simulator manifests and identifies the upstream Android storage failure behavior. A clean `npm ci` preserves the lockfile and patched qs/compiler versions. Gradle rejects that clean, unpatched dependency; `mobile:sync` then applies the source correction successfully and repeat application is harmless. Both mobile configuration/association checks pass. The initial Android compilation stopped at unaccepted SDK licences (`storage-android-compile.log`). After explicit user authorization, Platform 36, Build-Tools 35.0.0 and platform-tools were installed. `:app:assembleDebug :app:assembleDebugAndroidTest` then passed: 252 tasks. Evidence: `android-sdk-evidence.json`, `android-build.log`, `storage-patch-evidence.json`, `storage-unpatched-guard.log` and `storage-mobile-sync.log`.
- All three Android storage instrumentation tests pass on `heykeen_api36`, an AOSP API 36 ARM64 emulator (Emulator 37.1.11, image revision 2). They exercise a persisted encrypted round-trip, corrupted ciphertext, initialization failure without plaintext fallback, and failed disk commits. `:app:connectedDebugAndroidTest` succeeds with zero failures/errors/skips. This does not prove hardware-backed storage, physical process recovery, Google services or the mixed-client prototype. Evidence: `android-storage-tests.log`, `android-storage-tests.xml` and `android-artifact-evidence.json`.
- The iPhone 12 Pro Max is now connected by USB, paired, running iOS 26.6 (23G71), with Developer Mode enabled. Xcode successfully signs a separate **Heykeen Test** Debug artifact with the user's free Personal Team, and `devicectl` confirms installation. The signature verifies and the development profile includes this phone, expiring 15 September 2026. The first launch returned iOS's security/trust denial; after the user trusted their developer account, `devicectl` successfully launched the app. This artifact uses a temporary local-network Info.plist and omits Associated Domains for Personal Team signing; the project files were restored. Its authentication settings are deliberately blank. No physical journey, app-link or auth-callback pass is claimed. Evidence: `iphone-personal/build.log`, `artifact-evidence.json`, `install.json`, `launch.json` and `launch-after-trust.json`.
- For the physical/browser test, the ordinary browser UI created a Watch Session with a synthetic Host. iPhone Mirroring initially required Bluetooth and a locked phone. After the user supplied those, the phone screen became visible, but control actions failed with `noWindowsAvailable` or had no effect. The user was given the ordinary Join steps on the phone; network admission and the full round are still pending. The user subsequently reported the physical phone frozen on the app and unable to close it. Mirroring was stopped and the exact installed app process (PID 1109) was force-terminated over USB; a fresh process query confirms it is absent. The user force-restarted the phone and confirmed normal Home Screen use. The cause of the freeze remains unknown. Further physical launches were paused during investigation. The subsequent short input proof is recorded below; full physical journeys remain unverified.

## Resumed mixed-client verification — 8 September 2026

The physical iPhone 12 Pro Max (iOS 26.6), a headless Android API 36 emulator and the ordinary local browser UI joined Session FUKTF as three synthetic participants. Each completed the same 15-card Watch deck, liking Dunkirk and passing the other 14 titles; all three displayed **Perfect Match / Dunkirk**. The iPhone also exercised Like/Undo, background return, process termination, native sharing/cancellation, the Where to watch system-browser handoff, Host Restart, and Leave followed by a cold launch. Android restored the completed Match after force-stop/relaunch and returned Home after Leave/relaunch. Restart reset all three Ready confirmations. Neither sharing test sent anything to another person.

The initial iPhone cold relaunch revealed a server defect: rejoining with the same Socket.IO ID claimed its own display name and then removed the same Participant, deleting that claim. A subsequent new socket with the valid capability failed with DISPLAY_NAME_TAKEN. The shared join service now removes the prior entry only when the socket ID changes. A real HTTP/Socket.IO/Redis contract reproduced the failure before the fix and passes afterward. All four Lobby contracts and 133 related service/store tests pass; backend TypeScript compilation and diff whitespace checks pass. The original local test Session's already-missing claim was repaired explicitly as test setup before repeating the fixed background/relaunch cycle; this is not evidence of an automatic migration for damaged state.

A second iPhone Session (SQLWG) reached the Restaurant Lobby in under two seconds. The apparent location hang was the real iOS permission sheet, omitted from Appium's app-only accessibility snapshot but visible in its screenshot. Both the original binary and a metadata-only probe displayed that prompt, so the missing plist key did not cause that observation. The first denial initially remounted the Lobby during active-state reconciliation and lost its error/manual mode. Native lifecycle handling now uses Capacitor’s resume event (real foreground return on iOS), so permission-sheet dismissal does not unmount the form. The regression test failed before the change; ten lifecycle/recovery/guard tests and frontend typecheck pass afterward. A freshly installed JS bundle on the physical iPhone immediately shows the enabled manual field on its first denial, with no retry, and actual background return still reconnects. This retest used synthetic Session 3RR7Q. No successful restaurant lookup, granted location or approximate-mode result is claimed. The test app's location authorization was reset afterward.

The iOS source now includes the additional NSLocationAlwaysAndWhenInUseUsageDescription required by the [Geolocation plugin's documented setup](https://capacitorjs.com/docs/apis/geolocation), with the same foreground-use explanation. No background mode or background location request was added. The same metadata and the final lifecycle JS bundle were verified in a re-signed, installed copy of the Personal Team test artifact; plist validation, the Vite native bundle build and mobile configuration check pass. It is a setup correction, not a claimed fix for the permission-sheet observation.

Evidence: ignored `output/mobile/mixed-prototype/` contains native screenshots/XML, matching card traces, the red/green contract description, test logs and cleanup results. `output/mobile/location-probe/` records the initial metadata-only probe and the final installed lifecycle bundle; `probe-change.json` distinguishes them. All synthetic participants left, the temporary browser tab was closed, the iPhone returned to Home, and test app/runner/Appium/emulator processes were stopped. The local backend was rebuilt and restarted with the fix; Redis and the ordinary local browser dev server remain available. These checks do not pass the full physical iOS/Android/browser gate or the remaining identity/deletion/safety stories.

## Cook and expiry verification — 8 September 2026

The same physical iPhone and a browser completed Cook Session RGUXA as **iPhone Cook** and **Browser Cook**. The Host chose Italian, reduced the Deck from 15 to 5, and both reconfirmed Ready. Both liked Tuna Pasta Bake and explicitly chose **Finish here** at the unanimous-match prompt; both received that Match and the same Shopping List. This is the early-finish journey, not five completed swipes. The phone claimed penne, the browser claimed tomatoes, and each saw the other's Claim. Releasing the phone's penne Claim preserved the browser's tomatoes and $2.30 Tally. A separate browser tab opened the list without joining a Session and claimed as **Independent Shopper**.

The iPhone's checked first cooking Step stayed private from the browser and survived background return and process termination. Cold launch recovered the completed Match; opening Shopping List → Cook restored the checked Step. An initial short return displayed transient disconnect/rejoin-timeout notifications. A separate 161-second suspension, beyond the server's two-minute socket recovery window, settled successfully with exactly two Participants and preserved cooking progress. No code change was attributed to those transient notifications. The native list Share sheet displayed the public domain, and cancellation returned to the list without sending anything.

Only this synthetic local Session's Redis keys were accelerated to expiry. The list and method stayed available, and the independent Shopper successfully claimed another ingredient afterward. Accelerating the list's own expiry then exposed a shared frontend defect: a definitive HTTP 404 was treated like a temporary refresh failure, leaving old Claim controls visible. The shared list hook now removes unavailable lists on read or mutation failure, rejects stale read errors following newer changes, and rechecks settled Cook View data at its seven-day deadline. Temporary failures still preserve the recipe. Three new regression cases failed before the fix; all **87** related list, Cook View and wake-lock tests pass afterward. Frontend typecheck, browser/native bundles, Android Debug compilation and diff checks pass.

The updated JS was installed in a re-signed Personal Team iPhone artifact. Restoring only the synthetic list fixture allowed repeatable retests: an already-open list removed its controls on expiry, and a settled Cook View removed its Steps at the fixture's seven-day deadline without navigation. Both results also pass in the rebuilt browser bundle. For these isolated retests, Appium explicitly targeted the test bundle when opening the known list URL; this proves app routing, **not OS-verified Universal Link association**. The restored fixture used a shortened Redis TTL and, for the method check, an adjusted mint timestamp; these were accelerated expiry tests, not a natural seven-day wait. No Session or Participant was restored.

Evidence is in ignored `output/mobile/cook-prototype/`, including native XML/screenshots, the exact expiry injections, restored-fixture provenance, red/green test logs, signed install and Android build logs. The latest installed iPhone artifact is `cook-prototype/App.app`; the updated Android APK compiles but has not yet been exercised on the emulator. OS idle-timer behavior is still not independently verified: Appium can affect auto-lock, so screenshots taken under automation are not a wake-lock pass. Physical Android, real auth/provider callbacks, verified associations and the remaining spec gates remain outstanding. The synthetic Session, list and Claims are now absent, temporary browser tabs are closed, and the phone is back at Home with the test app, automation runner and Appium stopped. A final device inventory confirms **Heykeen Test 1.0 (build 1)** remains installed.

## Location and interruption verification — 8 September 2026

Synthetic Eat Out Session Y9ZVU joined the physical iPhone as **iPhone Nearby** (Host) and the API 36 Android emulator as **Android Nearby**. iPhone Allow Once with Precise Off supplied coarse coordinates. Android granted coarse location while fine location remained denied, using simulated Melbourne CBD GPS; both displayed the shared coordinate fallback. These are native permission/location checks, not a completed Eat Out round or successful Places lookup. This backend still used a deliberately dummy Places key. Existing local provider credentials were subsequently found, but their validity and live provider behavior have not yet been tested.

Android permission dismissal exposed a regression in the prior iOS lifecycle correction: Android emits resume after a permission sheet, remounting the Lobby and losing its manual-entry error. The shared lifecycle component now uses iOS resume and, on Android, requires an inactive-to-active transition before reconciliation. Android's first denial now preserves the error and manual form; entering 3000 enables Find area. A regression test failed before this change; all **16** focused lifecycle, recovery, guard and sharing tests pass afterward. Frontend typecheck, native JS builds, Android Debug compilation, iPhone signature verification and installation pass. The Lobby's native share button now correctly says **Share invite link**.

Android returned after 27 seconds in the background with Ready intact. Pausing only the local backend for 70 seconds showed reconnecting screens on both clients; resuming the same process automatically restored exactly two Participants, the original Host and Android Ready state. This proves recovery from a server interruption, not the full device-network matrix. Android's native share sheet showed the canonical public invite URL, and cancellation returned to the same Lobby without sending anything.

Evidence is in ignored `output/mobile/permissions-prototype/`, including red/green logs, native XML/screenshots, permission records, the guarded backend interruption and recovered roster. The latest installed iPhone artifact is now `permissions-prototype/App.app`, **Heykeen Test 1.0 (build 1)**; the rebuilt Android APK was installed and exercised on the emulator. Both synthetic Participants left, the iPhone returned to Home, and test app/runner/Appium/emulator processes were stopped. The local backend is healthy, with Redis and browser preview retained. A fresh device inventory confirms the iPhone app remains installed. The development app currently depends on the Mac's local backend; this is not a finished distribution build.

## Live providers, Comparison and Group Order — 8 September 2026

The existing local Google Places credential works. The first live Geocoding request for bare `3000` returned HTTP 200 without a result; `Melbourne VIC 3000 Australia` resolved. Google's [structured postal-address request](https://developers.google.com/maps/documentation/geocoding/geocoding#pass_a_structured_address) resolved both `3000` and `0800`. The shared lookup now uses that request for four-digit Australian postcodes, preserving leading zeroes and making one lookup. Two regression cases failed before the fix; all **71** search-service tests and backend compilation pass. Entering `3000` on the physical iPhone now displays Melbourne VIC 3000, Australia on both clients.

Eat Out Session **M3CQL** had two synthetic Participants, iPhone Dining and Browser Dining. Both completed all five choices, liking 11 Inch Pizza and passing the other four Restaurants, then submitted and received the same Match. Takeaway Session **TYBKH** used iPhone Takeaway and Browser Takeaway; both passed the first Restaurant, liked 11 Inch Pizza and explicitly chose **Finish here** at the unanimous-match prompt. That was an early finish after two choices, not five completed swipes. Both Sessions used live Places results. Native delivery links opened Uber Eats and DoorDash in the system browser and returned to the Match. Uber displayed a verification challenge; DoorDash displayed its app suggestion. No checkout or external order was performed.

A separate Docker Supabase project, `heykeen-mobile459`, applied the repository's unchanged migrations. Its API and Snapshot table were verified locally before enabling Comparison on the local backend. No production database or provider configuration was changed. One Comparison for 11 Inch Pizza was opened through both clients: DoorDash's 48-item menu arrived first, while Uber Eats remained loading, and both then displayed **40 matched items**. The local fetch observation recorded exactly one Apify request per platform (about 9.6 seconds for DoorDash and 26.5 seconds for Uber Eats), and the database contains one immutable Snapshot. Existing per-run spend limits remained in force. The iPhone retained the completed Comparison after 54 seconds at Home, and opening the Group Order reused the Snapshot without additional actor calls. A transient reconnect notification appeared on return; the Session recovered and retained its identity.

Both clients opened the Group Order. The browser added a $25.84 Hawaiian and the iPhone added a $23.00 Margherita; both displayed the $48.84 basket. The iPhone claimed Buyer, locking both views. A deliberately synthetic $4.00 fee produced a browser Share of $27.84, including $2.00 delivery. Native **Copy the split** displayed its success confirmation, and the Group Order's outbound link opened the system browser and returned. Clipboard contents were not independently read, and nothing was sent to another person.

Terminating and relaunching the iPhone retained the Buyer and stored fee, but exposed an empty fee input despite the shared amount remaining $4.00. The form now displays the authoritative fee until the Buyer edits it. A second regression also proves that invalid input cancels an earlier unsent fee update. Both cases failed before the fix; all **38** Group Order/money tests pass. Frontend typecheck, browser/iPhone/Android bundles, Android Debug compilation and signature verification pass. The updated iPhone was installed and relaunched: the field now displays **4.00**, the Group Order and handoff remain available, and a reloaded browser retains its $27.84 Share.

Evidence is under ignored `output/mobile/provider-prototype/`. The latest installed iPhone artifact is `provider-prototype/App.app`, still **Heykeen Test 1.0 (build 1)**. The Android APK contains the fee correction and compiles, but this latest change has not been exercised on Android. Both synthetic Participants left, the temporary browser tab closed, and the phone app, automation runner and Appium stopped. The local backend, Redis, browser preview and isolated Supabase database remain available; the single immutable Snapshot is retained locally. The full spec is still incomplete, particularly physical Android, identity/deletion/safety, verified associations, accessibility and signed release engineering.

## Prototype findings addressed

Control tooling update, 8 September 2026: AXe 1.8.0 passes simulator taps, swipe, text entry and Home navigation with Simulator.app closed. Appium 3.7.0/XCUITest 12.10.0 also passes these controls on the iPhone 12 Pro Max with the free Personal Team runner. Enabling the separate iPhone UI Automation switch resolved its initial runner startup timeout. On both simulator and physical iPhone, the existing app opens Watch setup, accepts a name and navigates back. The real phone returns to SpringBoard; the test app, runner and session were stopped afterward. No Session was submitted. Evidence and limitations are in [mobile control research](mobile-control-tools-research.md) and ignored `output/mobile/control-proof/`. Android driver/toolchain checks pass but physical input remains unverified. The control obstacle is bypassed; the full physical mixed-client prototype and cause of the earlier phone freeze remain open.

1. An unsigned simulator binary could open the interface but could not write its rejoin credential. Local ad-hoc signing supplies simulated identity; secure persistence then survived process termination. Use the signed-to-run simulator command in [mobile development](mobile-development.md). This is not distribution signing.
2. Home initially overlapped the status area; automatic WebView insets also doubled the Session inset on cold launch. CSS safe areas now own the inset, and route announcements retain focus without scrolling the header under system UI.
3. Native invite sharing initially inherited the local backend’s browser URL. The shared Session HTTP client now supplies the configured canonical public Invite Link to both Lobby and Deck sharing.
4. Native lifecycle listeners previously depended on the router’s changing navigate function, which could replay a cold-launch link after navigation. Listeners now live for the native app mount and read the latest navigation function.
5. The system share preview currently inherits a different brand name from the live website. Final public branding and metadata still need reconciliation; no live site was changed.
6. Native bridge diagnostics can log secure-plugin arguments. Bridge logging is disabled; do not enable it against real credentials.
7. The Android storage dependency could silently fall back to unencrypted SharedPreferences and hide read/write failures. A pinned source patch removes that fallback and propagates errors; `mobile:sync` applies it and Gradle refuses an unpatched helper. Encrypted data format and the existing adapter remain unchanged. All three encryption/failure instrumentation tests pass on the API 36 emulator; physical-device verification remains pending.
8. Default HTTP access logging exposed headers and full URLs; explicit list logs also included bearer list IDs and Shopper labels. Shared backend logging now retains request correlation/status while excluding these values, and Caddy filters private request/response fields. Thirteen HTTP logging/request-ID tests, 28 real Redis Shopping List tests and the Caddy serving/privacy contract pass. Typecheck passes. This is a local correction; hosting logs and prior deployments remain outside that proof.

## Dependencies

React Router 7.18.3 fixes the navigation advisory; Express’s transitive `qs` is scoped to 6.16.0 until its upstream range includes that fix. The root compiler is pinned to TypeScript 5.9.3 so an analysis tool’s transitive TypeScript 7 cannot take over the repository’s `npx tsc` commands. The final `npm ci` also passed an explicit check that Express loads with qs 6.16.0 and the root compiler is 5.9.3. A clean lockfile/install was required because npm’s workspace override handling retained or dropped the old dependency incorrectly. `npm ci` installs the corrected graph and Express loads. `npm ls qs` can still mislabel that valid overridden edge as invalid; this matches [npm’s open workspace override issue](https://github.com/npm/cli/issues/9514). Recheck the actual installed version, backend tests and audit after any lockfile update. Remove the override when upstream supports the patched version.

The production-only audit reports zero advisories after the updates. Development dependencies still have advisories and need a separate toolchain upgrade before release CI is considered ready. Sources: [router advisory](https://github.com/remix-run/react-router/security/advisories/GHSA-wrjc-x8rr-h8h6), [qs advisory](https://github.com/ljharb/qs/security/advisories/GHSA-4mjr-xmp4-gh2g).

## Required operator inputs and gate

- Keep the Mac and iPhone unlocked for initial native setup.
- The iPhone's guest Branch journeys, native control, granted approximate location, streamed Comparison and Group Order handoff have recorded evidence. The user confirmed no Android phone is available. Emulator evidence does not replace the required physical Android test; real system-browser authentication callbacks and the remaining native matrix are still outstanding.
- The user confirmed only a free Apple Personal Team. Basic development signing can be prepared, but Associated Domains and Sign in with Apple require paid program access before those checks can pass. [Apple capability availability](https://developer.apple.com/help/account/reference/supported-capabilities-ios/).
- The canonical public domain is `https://yupcrew.com`; verify permanent package IDs, legal publishing identity and signing access before distribution builds.
- Google/Apple/Supabase redirect/provider configuration in an isolated test environment before identity proof. No live provider configuration or production service was changed.

The spec explicitly puts the physical iPhone/Android/browser prototype before expanding into the remaining identity, deletion and safety migration. Complete that gate with the [development checklist](mobile-development.md), then continue the outstanding stories. Store publication remains excluded from this implementation request.
