node_modules
dist/
coverage/
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.specify
.cursor
# Backend's tsc build emits these next to the generated-types source (it sits
# outside backend/src's rootDir). The committed database.types.ts is the authority.
supabase/database.types.js
supabase/database.types.d.ts
supabase/database.types.*.map
.playwright-cli
output/
# Owned Recipe hero images (#330): masters and WebPs are working files on
# their way to R2. The record references the image by URL; the bytes never
# enter the repository.
.corpus-images/
docs/production-cold-load-cdn-plan.md
