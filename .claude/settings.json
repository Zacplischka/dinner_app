{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR/.claude/hooks/format-on-edit.sh\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && CHANGED=$( { git diff --name-only HEAD -- '*.ts' '*.tsx'; git ls-files --others --exclude-standard -- '*.ts' '*.tsx'; } 2>/dev/null); [ -z \"$CHANGED\" ] && exit 0; OUT=$(npm run lint 2>&1) || { printf '%s\\n' \"$OUT\" | tail -40 >&2; exit 2; }",
            "asyncRewake": true,
            "timeout": 120,
            "statusMessage": "Linting changed TypeScript"
          }
        ]
      }
    ]
  }
}