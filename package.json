{
  "name": "dinder",
  "version": "1.0.0",
  "description": "Dinder - Real-time group dinner decision making",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=backend & npm run dev --workspace=frontend",
    "build": "npm run build --workspace=shared && npm run build --workspace=backend && npm run build --workspace=frontend",
    "build:frontend": "npm run build --workspace=shared && npm run build --workspace=frontend",
    "check:frontend-serving": "bash scripts/check-frontend-serving.sh",
    "check": "node --test 'scripts/**/*.test.mjs'",
    "start": "node backend/dist/server.js",
    "test": "npm run test --workspace=backend && npm run test --workspace=frontend",
    "typecheck": "npm run build --workspace=shared && npx tsc --noEmit -p backend && npx tsc --noEmit -p backend/tsconfig.test.json && npx tsc --noEmit -p frontend && npx tsc --noEmit -p frontend/tsconfig.test.json",
    "test:e2e:live": "npm run test:e2e:live --workspace=frontend --",
    "compare:smoke": "npm run compare:smoke --workspace=backend --",
    "lint": "npm run lint --workspace=backend && npm run lint --workspace=frontend",
    "format": "npm run format --workspace=backend && npm run format --workspace=frontend",
    "analyze": "fallow --production-dead-code",
    "analyze:pr": "fallow audit --base origin/main",
    "gen:types": "supabase gen types typescript --project-id hcjuqvicwuszwqkreklc --schema public | npx prettier --parser typescript > supabase/database.types.ts"
  },
  "keywords": [
    "dinder",
    "dinner",
    "restaurant",
    "group-decision"
  ],
  "author": "Zac Plischka",
  "license": "MIT",
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.122.0",
    "@typescript-eslint/eslint-plugin": "~8.39.0",
    "@typescript-eslint/parser": "~8.39.0",
    "eslint": "^8.56.0",
    "fallow": "^3.3.0",
    "prettier": "^3.1.1",
    "typescript": "5.9.3",
    "vitest": "~4.1.11"
  },
  "overrides": {
    "express": {
      "qs": "6.16.0",
      "body-parser": {
        "qs": "6.16.0"
      }
    },
    "vite": "~6.4.3"
  },
  "engines": {
    "node": "22.x"
  }
}
