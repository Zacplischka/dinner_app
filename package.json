{
  "name": "dinder",
  "version": "1.0.0",
  "description": "Dinder - Real-time group dinner decision making",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=backend & npm run dev --workspace=frontend",
    "build": "npm run build --workspace=shared && npm run build --workspace=backend && npm run build --workspace=frontend && npm run postbuild",
    "postbuild": "cp backend/src/redis/*.lua backend/dist/redis/",
    "test": "npm run test --workspace=backend && npm run test --workspace=frontend",
    "lint": "npm run lint --workspace=backend && npm run lint --workspace=frontend",
    "format": "npm run format --workspace=backend && npm run format --workspace=frontend"
  },
  "keywords": [
    "dinder",
    "dinner",
    "restaurant",
    "group-decision"
  ],
  "author": "Dinder Team",
  "license": "MIT",
  "devDependencies": {
    "canvas": "^3.2.0"
  }
}
